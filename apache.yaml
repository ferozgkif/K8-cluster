apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-apache-7
  namespace: feroz
spec:
  selector:
    matchLabels:
      app: apache
  replicas: 1
  template:
    metadata:
      labels:
        app: apache

    spec:
      containers:
      - name: apache
        image: ferozfrk/apache:7
        ports:
        - containerPort: 80
